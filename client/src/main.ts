// Simple entry point to prevent Vite errors
// The actual Angular dashboard is running from index.html
console.log('AquaFlow Dashboard - Entry point loaded');

// Prevent any potential undefined errors
export {};